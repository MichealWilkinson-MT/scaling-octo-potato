/**
 * Below is an example of an object with 3 parameters
 */

let exampleObject = {
    param1: 1,
    param2: "The second parameter",
    param3: [
        "justSomeStrings",
        "inAnArray"
    ]
};

/**
 * To retrieve a parameter from an object 
 * exampleObject.param1
 * 
 * Use the console.log below to print out param2 of the exampleObject 
 */

console.log();

/**
 * You can modify and add parameters to an object in a similar way to the way you 
 * would access them, for this step of the exercise, add a parameter to the 
 * exampleObject called fooBar with the value "Hello World" (thanks to the code 
 * at the bottom of this file this should be printed with this file is run)
 * 
 * example:
 * exampleObject.param2 = "This new value would replace param2"
 */


// You should not change anything below this line
if (undefined != exampleObject.fooBar) {
    console.log(exampleObject.fooBar)
}